import static org.stackwork.gradle.docker.ModuleType.*

plugins {
  id "org.stackwork" version "0.9.0-rc.1"
  id "groovy"
}

repositories {
  jcenter()
}

project.version = 1.0

stackwork {
  moduleType = DELIVERABLE_IMAGE
  imageName = 'qkrijger/nettools'
}
